// Generated by tools/generator/generate_kernel_traits.py. Do not edit.
// Source: tools/generator/kernel_traits.yaml

inline FwdKernelMeta select_fwd_meta(const Flash_fwd_params &params) {
    const int arch = params.arch;
    const int headdim = fwd_effective_headdim(params.d);
    const bool is_dropout = params.p_dropout < 1.0f;
    const bool is_mla = headdim == 192 && params.d_value_rounded == 128;
    if (arch == Arch::xcore1000 && headdim == 32) { return make_fwd_meta(Arch::xcore1000, 32, 32, 128, 128, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 64) { return make_fwd_meta(Arch::xcore1000, 64, 64, 64, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 96) { return make_fwd_meta(Arch::xcore1000, 96, 96, 128, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 128) { return make_fwd_meta(Arch::xcore1000, 128, 128, 64, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 160) { return make_fwd_meta(Arch::xcore1000, 160, 160, 64, 32, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 192 && is_mla) { return make_fwd_meta(Arch::xcore1000, 192, 128, 128, 64, 8, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 192 && !is_mla) { return make_fwd_meta(Arch::xcore1000, 192, 192, 64, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 256 && is_dropout) { return make_fwd_meta(Arch::xcore1000, 256, 256, 64, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 256 && !is_dropout) { return make_fwd_meta(Arch::xcore1000, 256, 256, 64, 32, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1000 && headdim == 512) { return make_fwd_meta(Arch::xcore1000, 512, 512, 64, 32, 8, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 32 && is_dropout) { return make_fwd_meta(Arch::xcore1500, 32, 32, 128, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 32 && !is_dropout) { return make_fwd_meta(Arch::xcore1500, 32, 32, 128, 128, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 64) { return make_fwd_meta(Arch::xcore1500, 64, 64, 128, 64, 4, false, false, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 96) { return make_fwd_meta(Arch::xcore1500, 96, 96, 128, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 128) { return make_fwd_meta(Arch::xcore1500, 128, 128, 128, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 160) { return make_fwd_meta(Arch::xcore1500, 160, 160, 128, 64, 4, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 192 && is_mla) { return make_fwd_meta(Arch::xcore1500, 192, 128, 128, 64, 8, true, true, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 192 && !is_mla) { return make_fwd_meta(Arch::xcore1500, 192, 192, 128, 64, 4, false, false, 5, 0, 2); }
    if (arch == Arch::xcore1500 && headdim == 256) { return make_fwd_meta(Arch::xcore1500, 256, 256, 128, 64, 4, false, false, 5, 0, 2); }
    return {};
}

inline FwdSplitKernelMeta select_fwd_split_meta(const Flash_fwd_params &params) {
    const int arch = params.arch;
    const int headdim = fwd_split_effective_headdim(params.d);
    if (arch == Arch::xcore1000 && headdim == 32) { return make_fwd_split_meta(Arch::xcore1000, 32, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1000 && headdim == 64 && params.seqlen_q <= 16) { return make_fwd_split_meta(Arch::xcore1000, 64, 16, 16, 1, true, true, 16); }
    if (arch == Arch::xcore1000 && headdim == 64) { return make_fwd_split_meta(Arch::xcore1000, 64, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1000 && headdim == 96) { return make_fwd_split_meta(Arch::xcore1000, 96, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1000 && headdim == 128 && params.seqlen_q <= 16) { return make_fwd_split_meta(Arch::xcore1000, 128, 16, 16, 1, true, true, 8); }
    if (arch == Arch::xcore1000 && headdim == 128 && params.seqlen_q <= 32) { return make_fwd_split_meta(Arch::xcore1000, 128, 32, 32, 2, true, true, 4); }
    if (arch == Arch::xcore1000 && headdim == 128) { return make_fwd_split_meta(Arch::xcore1000, 128, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1000 && headdim == 160) { return make_fwd_split_meta(Arch::xcore1000, 160, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1000 && headdim == 192) { return make_fwd_split_meta(Arch::xcore1000, 192, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1000 && headdim == 256) { return make_fwd_split_meta(Arch::xcore1000, 256, 64, 32, 4, true, true, 2); }
    if (arch == Arch::xcore1000 && headdim == 512) { return make_fwd_split_meta(Arch::xcore1000, 512, 32, 32, 2, true, true, 2); }
    if (arch == Arch::xcore1500 && headdim == 32) { return make_fwd_split_meta(Arch::xcore1500, 32, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1500 && headdim == 64) { return make_fwd_split_meta(Arch::xcore1500, 64, 128, 64, 4, false, false, 2); }
    if (arch == Arch::xcore1500 && headdim == 96) { return make_fwd_split_meta(Arch::xcore1500, 96, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1500 && headdim == 128 && params.seqlen_q <= 32) { return make_fwd_split_meta(Arch::xcore1500, 128, 16, 32, 1, true, true, 8); }
    if (arch == Arch::xcore1500 && headdim == 128) { return make_fwd_split_meta(Arch::xcore1500, 128, 128, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1500 && headdim == 160) { return make_fwd_split_meta(Arch::xcore1500, 160, 64, 64, 4, true, true, 2); }
    if (arch == Arch::xcore1500 && headdim == 192) { return make_fwd_split_meta(Arch::xcore1500, 192, 128, 64, 4, false, false, 2); }
    if (arch == Arch::xcore1500 && headdim == 256) { return make_fwd_split_meta(Arch::xcore1500, 256, 128, 64, 4, false, false, 2); }
    return {};
}

#define FWD_META_SWITCH(META, kArch, kHeadDim, kHeadDimV, kBlockM, kBlockN, kNWarps, Is_Q_in_regs, Share_Q_K_smem, ...) \
    [&] { \
        const auto &meta__ = (META); \
        mcFlashAttn::check_fwd_meta_supported(meta__); \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 32 && (kHeadDimV) == 32) { \
            if (meta__.block_m == 128 && meta__.block_n == 128 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 128; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 32 && (kHeadDimV) == 32) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 64 && (kHeadDimV) == 64) { \
            if (meta__.block_m == 16 && meta__.block_n == 16 && meta__.nwarps == 1 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 16; \
                constexpr static int kBlockN = 16; \
                constexpr static int kNWarps = 1; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 64 && (kHeadDimV) == 64) { \
            if (meta__.block_m == 32 && meta__.block_n == 32 && meta__.nwarps == 2 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 32; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 2; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 64 && (kHeadDimV) == 64) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 64 && (kHeadDimV) == 64) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 64 && (kHeadDimV) == 64) { \
            if (meta__.block_m == 128 && meta__.block_n == 128 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 128; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 96 && (kHeadDimV) == 96) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 96 && (kHeadDimV) == 96) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 32 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 64 && meta__.block_n == 32 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 160 && (kHeadDimV) == 160) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 160 && (kHeadDimV) == 160) { \
            if (meta__.block_m == 64 && meta__.block_n == 32 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 160 && (kHeadDimV) == 160) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 192 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 8 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 8; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 192 && (kHeadDimV) == 192) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 256 && (kHeadDimV) == 256) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 256 && (kHeadDimV) == 256) { \
            if (meta__.block_m == 64 && meta__.block_n == 32 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 512 && (kHeadDimV) == 512) { \
            if (meta__.block_m == 64 && meta__.block_n == 32 && meta__.nwarps == 8 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 8; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 32 && (kHeadDimV) == 32) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 32 && (kHeadDimV) == 32) { \
            if (meta__.block_m == 128 && meta__.block_n == 128 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 128; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 64 && (kHeadDimV) == 64) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == false && meta__.share_q_k_smem == false) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = false; \
                constexpr static bool Share_Q_K_smem = false; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 96 && (kHeadDimV) == 96) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 128 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == false && meta__.share_q_k_smem == false) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = false; \
                constexpr static bool Share_Q_K_smem = false; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 128 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 160 && (kHeadDimV) == 160) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 192 && (kHeadDimV) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 8 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 8; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 192 && (kHeadDimV) == 192) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == false && meta__.share_q_k_smem == false) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = false; \
                constexpr static bool Share_Q_K_smem = false; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 256 && (kHeadDimV) == 256) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == false && meta__.share_q_k_smem == false) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = false; \
                constexpr static bool Share_Q_K_smem = false; \
                return __VA_ARGS__(); \
            } \
        } \
        throw std::invalid_argument("Unsupported generated fwd kernel tuple"); \
    }()

#define FWD_SPLIT_META_SWITCH(META, kArch, kHeadDim, kBlockM, kBlockN, kNWarps, Is_Q_in_regs, Share_Q_K_smem, ...) \
    [&] { \
        const auto &meta__ = (META); \
        mcFlashAttn::check_fwd_split_meta_supported(meta__); \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 32) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 64) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 64) { \
            if (meta__.block_m == 16 && meta__.block_n == 16 && meta__.nwarps == 1 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 16; \
                constexpr static int kBlockN = 16; \
                constexpr static int kNWarps = 1; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 96) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128) { \
            if (meta__.block_m == 64 && meta__.block_n == 32 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128) { \
            if (meta__.block_m == 32 && meta__.block_n == 32 && meta__.nwarps == 2 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 32; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 2; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 128) { \
            if (meta__.block_m == 16 && meta__.block_n == 16 && meta__.nwarps == 1 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 16; \
                constexpr static int kBlockN = 16; \
                constexpr static int kNWarps = 1; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 160) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 192) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 256) { \
            if (meta__.block_m == 64 && meta__.block_n == 32 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 256) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1000 && (kHeadDim) == 512) { \
            if (meta__.block_m == 32 && meta__.block_n == 32 && meta__.nwarps == 2 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 32; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 2; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 32) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 64) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == false && meta__.share_q_k_smem == false) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = false; \
                constexpr static bool Share_Q_K_smem = false; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 96) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 128) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 128) { \
            if (meta__.block_m == 16 && meta__.block_n == 32 && meta__.nwarps == 1 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 16; \
                constexpr static int kBlockN = 32; \
                constexpr static int kNWarps = 1; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 160) { \
            if (meta__.block_m == 64 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == true && meta__.share_q_k_smem == true) { \
                constexpr static int kBlockM = 64; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = true; \
                constexpr static bool Share_Q_K_smem = true; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 192) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == false && meta__.share_q_k_smem == false) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = false; \
                constexpr static bool Share_Q_K_smem = false; \
                return __VA_ARGS__(); \
            } \
        } \
        if constexpr ((kArch) == Arch::xcore1500 && (kHeadDim) == 256) { \
            if (meta__.block_m == 128 && meta__.block_n == 64 && meta__.nwarps == 4 && meta__.is_q_in_regs == false && meta__.share_q_k_smem == false) { \
                constexpr static int kBlockM = 128; \
                constexpr static int kBlockN = 64; \
                constexpr static int kNWarps = 4; \
                constexpr static bool Is_Q_in_regs = false; \
                constexpr static bool Share_Q_K_smem = false; \
                return __VA_ARGS__(); \
            } \
        } \
        throw std::invalid_argument("Unsupported generated fwd kernel tuple"); \
    }()
